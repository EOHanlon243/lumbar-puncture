- name: Send Email
  uses: wadeww/send-email-action@master
  if: failure()
  with:
    server_address: fundamentalsurgery.com
    port: 465
    username: ${{ secrets.MAIL_USERNAME }}
    password: ${{ secrets.MAIL_PASSWORD }}
    subject: "Failed Build for ${{ matrix.projectName }}"
    body: "An automated build for ${{ github.repository }} failed to complete. Please refer to github.com/${{ github.repository }}/actions for details."
    to: engineering@fundamentalvr.com
    from: builds@fundamentalsurgery.com