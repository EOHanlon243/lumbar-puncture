- name: Checkout 
  if: env.REPO_ALIVE == 1
  uses: actions/checkout@v2
  with:
    token: ${{ secrets.FVR_CI }}
    lfs: true
    submodules: true
    clean: false
    ref: ${{ matrix.branchName }}
    path: ./${{ matrix.workFolder }}